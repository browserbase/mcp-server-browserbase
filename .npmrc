# Use npm for package management
engine-strict=true

# Platform-specific settings to prevent sharp from installing binaries for all architectures
target_platform=linux
target_arch=x64
target_libc=glibc
